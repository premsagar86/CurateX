// Client dashboard — PLAN.md §20.14.
export default function DashboardPage() {
  return (
    <div>
      <h1 className="font-display text-2xl">Dashboard</h1>
      {/* TODO: active project count, next milestone due, invoice due, project
          summary cards, recent activity feed — PLAN.md §20.14 */}
    </div>
  );
}

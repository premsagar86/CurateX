// Admin dashboard — PLAN.md §20.20. Task-prioritization view, not a data browser.
export default function AdminDashboardPage() {
  return (
    <div>
      <h1 className="font-display text-2xl">Admin</h1>
      {/* TODO: "Needs your attention" panel (unresponded leads, milestones
          awaiting QA, overdue invoices), pipeline snapshot, WIP-per-founder
          indicator — PLAN.md §20.20, §34.3 */}
    </div>
  );
}
